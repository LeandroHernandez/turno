export enum RoutesApp {
  root = '',
  auth = 'auth',
  logIn = 'log-in',
  register = 'sign-up',
  home = 'home',
  map = 'map',
  view_map = 'preview-map',
  profile = 'profile',
  // account = 'account',
}
